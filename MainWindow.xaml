<Window x:Class="VRChatSpeechAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="VRChat 语音识别助手" Height="600" Width="700"
        Background="#1a1a2e" WindowStartupLocation="CenterScreen"
        Foreground="White">
    
    <Window.Resources>
        <!-- 现代深色按钮样式 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#7c4dff"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#9575cd"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#673ab7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 次要按钮样式 -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2d2d44"/>
            <Setter Property="Foreground" Value="#b0bec5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3d3d5c"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3d3d5c"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 现代复选框样式 -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="checkBorder" Width="22" Height="22" 
                                    Background="#2d2d44" BorderBrush="#3d3d5c" 
                                    BorderThickness="2" CornerRadius="4" Margin="0,0,10,0">
                                <TextBlock x:Name="checkMark" Text="✓" FontSize="14" 
                                           Foreground="#7c4dff" FontWeight="Bold"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="checkBorder" Property="BorderBrush" Value="#7c4dff"/>
                                <Setter TargetName="checkBorder" Property="Background" Value="#3d2d5c"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="checkBorder" Property="BorderBrush" Value="#9575cd"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 卡片样式 -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- 标签样式 -->
        <Style x:Key="Label" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#9e9e9e"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部控制栏 -->
        <Border Grid.Row="0" Background="#16213e" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧：状态指示器 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,24,0">
                    <Border x:Name="StatusIndicator" Width="10" Height="10" CornerRadius="5"
                            Background="#4CAF50" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TxtServerStatus" Text="运行中"
                               FontWeight="SemiBold" Foreground="#4CAF50" FontSize="14"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- 中间：快速开关 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <CheckBox x:Name="ChkOscEnabled" Content="OSC推送"
                              Style="{StaticResource ModernCheckBox}" Margin="0,0,16,0"
                              Checked="ChkOscEnabled_Changed" Unchecked="ChkOscEnabled_Changed"/>
                    <CheckBox x:Name="ChkOscRealtime" Content="实时输入"
                              Style="{StaticResource ModernCheckBox}" Margin="0,0,16,0"
                              Checked="ChkOscRealtime_Changed" Unchecked="ChkOscRealtime_Changed"/>
                    <CheckBox x:Name="ChkVrcSync" Content="VRC-mic同步"
                              Style="{StaticResource ModernCheckBox}"
                              Checked="ChkVrcSync_Changed" Unchecked="ChkVrcSync_Changed"/>
                </StackPanel>

                <!-- 右侧：按钮 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="⚙️ 设置"
                            Click="BtnSettings_Click"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0"/>
                    <Button Content="📋 日志"
                            Click="BtnLog_Click"
                            Style="{StaticResource SecondaryButton}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- WebView2 内容区域 -->
        <wv2:WebView2 x:Name="webView"
                      Grid.Row="1"
                      DefaultBackgroundColor="#1a1a2e"/>
    </Grid>
</Window>