<Window x:Class="VRChatSpeechAssistant.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置" Height="500" Width="500"
        Background="#1a1a2e" WindowStartupLocation="CenterOwner"
        Foreground="White" ResizeMode="NoResize">
    
    <Window.Resources>
        <!-- 现代化文本框样式 -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Background" Value="#2d2d44"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3d3d5c"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CaretBrush" Value="#7c4dff"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#7c4dff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 现代复选框样式 -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="checkBorder" Width="22" Height="22" 
                                    Background="#2d2d44" BorderBrush="#3d3d5c" 
                                    BorderThickness="2" CornerRadius="4" Margin="0,0,10,0">
                                <TextBlock x:Name="checkMark" Text="✓" FontSize="14" 
                                           Foreground="#7c4dff" FontWeight="Bold"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="checkBorder" Property="BorderBrush" Value="#7c4dff"/>
                                <Setter TargetName="checkBorder" Property="Background" Value="#3d2d5c"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="checkBorder" Property="BorderBrush" Value="#9575cd"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 标签样式 -->
        <Style x:Key="Label" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#9e9e9e"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <!-- 分组标题样式 -->
        <Style x:Key="GroupTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- 按钮样式 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#7c4dff"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#9575cd"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="⚙️ 设置" FontSize="24" FontWeight="Bold" Margin="0,0,0,24"/>
        
        <!-- 设置内容 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- OSC 设置 -->
                <Border Background="#16213e" CornerRadius="12" Padding="20" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="📡 OSC 设置" Style="{StaticResource GroupTitle}"/>
                        
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="目标 IP:" Style="{StaticResource Label}" VerticalAlignment="Center"/>
                            <TextBox x:Name="TxtOscIp" Grid.Column="1" Style="{StaticResource ModernTextBox}"/>
                        </Grid>
                        
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="目标端口:" Style="{StaticResource Label}" VerticalAlignment="Center"/>
                            <TextBox x:Name="TxtOscPort" Grid.Column="1" Style="{StaticResource ModernTextBox}"/>
                        </Grid>
                        
                        <CheckBox x:Name="ChkOscEnabled" Content="启用 OSC 发送" 
                                  Style="{StaticResource ModernCheckBox}" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- VRC 设置 -->
                <Border Background="#16213e" CornerRadius="12" Padding="20" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="🎮 VRChat 设置" Style="{StaticResource GroupTitle}"/>
                        
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="监听端口:" Style="{StaticResource Label}" VerticalAlignment="Center"/>
                            <TextBox x:Name="TxtListenPort" Grid.Column="1" Style="{StaticResource ModernTextBox}"/>
                        </Grid>
                        
                        <CheckBox x:Name="ChkVrcSync" Content="启用 VRC 麦克风同步" 
                                  Style="{StaticResource ModernCheckBox}"/>
                        <TextBlock Text="当 VRChat 麦克风静音时，暂停 OSC 发送" 
                                   Foreground="#78909c" FontSize="12" Margin="32,4,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- 服务器端口设置 -->
                <Border Background="#16213e" CornerRadius="12" Padding="20" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="🌐 服务器端口设置" Style="{StaticResource GroupTitle}"/>
                        
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="HTTP 端口:" Style="{StaticResource Label}" VerticalAlignment="Center"/>
                            <TextBox x:Name="TxtHttpPort" Grid.Column="1" Style="{StaticResource ModernTextBox}"/>
                        </Grid>
                        <TextBlock Text="Web 界面访问端口（默认: 3000）"
                                   Foreground="#78909c" FontSize="12" Margin="0,0,0,0"/>
                    </StackPanel>
                </Border>
                
            </StackPanel>
        </ScrollViewer>
        
        <!-- 按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
            <Button Content="保存" Style="{StaticResource ModernButton}" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>